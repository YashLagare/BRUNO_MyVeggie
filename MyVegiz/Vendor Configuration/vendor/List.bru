meta {
  name: List
  type: http
  seq: 2
}

get {
  url: {{base_url2}}/vendor/vendor/list
  body: none
  auth: bearer
}

params:query {
  ~q: 7
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
