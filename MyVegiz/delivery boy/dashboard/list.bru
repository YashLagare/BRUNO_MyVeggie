meta {
  name: list
  type: http
  seq: 1
}

get {
  url: {{base_url2}}/delivery_boy/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{delivery_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
