meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{base_url2}}/admin/delivery_charges
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  free_km: 5
  per_km_charge: 10
}

settings {
  encodeUrl: true
  timeout: 0
}
