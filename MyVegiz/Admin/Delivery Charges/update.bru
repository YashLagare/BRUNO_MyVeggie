meta {
  name: update
  type: http
  seq: 3
}

put {
  url: {{base_url2}}/admin/delivery_charges
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  free_km: 6
  per_km_charge: 11
  is_active: true
}

settings {
  encodeUrl: true
  timeout: 0
}
