meta {
  name: category dropdown
  type: http
  seq: 5
}

get {
  url: {{base_url2}}/admin/sub-categories/dropdown
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
