meta {
  name: list
  type: http
  seq: 3
}

get {
  url: {{base_url2}}/admin/sub-categories/list?page=1&limit=10
  body: multipartForm
  auth: bearer
}

params:query {
  page: 1
  limit: 10
  ~q: gnic
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
