meta {
  name: list
  type: http
  seq: 1
}

get {
  url: {{base_url}}admin/main-categories/list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
