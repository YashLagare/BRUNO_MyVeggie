meta {
  name: zones dropdown
  type: http
  seq: 5
}

get {
  url: {{base_url2}}/admin/product-variants/zones
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
