meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{base_url2}}/admin/delivery_boy/commissions
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  per_km_commission: 5
}

settings {
  encodeUrl: true
  timeout: 0
}
