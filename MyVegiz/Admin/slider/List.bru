meta {
  name: List
  type: http
  seq: 2
}

get {
  url: {{base_url2}}/admin/slider/list?q=home&page=1&limit=10
  body: none
  auth: bearer
}

params:query {
  q: home
  page: 1
  limit: 10
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
