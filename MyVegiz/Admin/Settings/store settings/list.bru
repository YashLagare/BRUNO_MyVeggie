meta {
  name: list
  type: http
  seq: 2
}

get {
  url: {{base_url2}}/admin/store_settings
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
