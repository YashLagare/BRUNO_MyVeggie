meta {
  name: create
  type: http
  seq: 1
}

get {
  url: {{base_url2}}/admin/email-settings
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
