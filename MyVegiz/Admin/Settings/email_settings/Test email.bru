meta {
  name: Test email
  type: http
  seq: 4
}

post {
  url: {{base_url2}}/admin/email-settings/test
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  to_email: dovesek872@naprb.com
  subject: Test Mail
  message: Hello from MyVegiz
  : 
}

settings {
  encodeUrl: true
  timeout: 0
}
