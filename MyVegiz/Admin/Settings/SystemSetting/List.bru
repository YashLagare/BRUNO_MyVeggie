meta {
  name: List
  type: http
  seq: 1
}

get {
  url: {{base_url2}}/admin/system_settings/list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
