meta {
  name: Zone (Dropdown)
  type: http
  seq: 6
}

get {
  url: {{base_url2}}/admin/delivery/zones/dropdown
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
