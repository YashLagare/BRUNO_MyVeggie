meta {
  name: delete
  type: http
  seq: 3
}

put {
  url: {{base_url2}}/admin/zone/delete?zone_id=7
  body: multipartForm
  auth: bearer
}

params:query {
  zone_id: 7
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
