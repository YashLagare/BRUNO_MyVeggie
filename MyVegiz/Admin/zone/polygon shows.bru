meta {
  name: polygon shows
  type: http
  seq: 6
}

get {
  url: {{base_url2}}/admin/zone/polygons
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
