meta {
  name: List
  type: http
  seq: 2
}

get {
  url: {{base_url2}}/admin/vendor/list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
