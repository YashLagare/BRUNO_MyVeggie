meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{base_url2}}/admin/vendor/create
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  entity_category: ABC
  main_category_id: 17
}

settings {
  encodeUrl: true
  timeout: 0
}
