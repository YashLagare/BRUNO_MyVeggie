meta {
  name: List (ACTIVE DISCOUNT for product_variant)
  type: http
  seq: 5
}

get {
  url: {{base_url2}}/admin/product/discount/active/31
  body: none
  auth: bearer
}

params:query {
  ~q: discount
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
