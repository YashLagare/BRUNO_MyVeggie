meta {
  name: Dropdown(ProductVariants)
  type: http
  seq: 6
}

get {
  url: {{base_url2}}/admin/product/discount/variants
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
