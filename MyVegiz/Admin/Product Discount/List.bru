meta {
  name: List
  type: http
  seq: 2
}

get {
  url: {{base_url2}}/admin/product/discount/list?page=1&limit=10
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 10
  ~q: discount
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
