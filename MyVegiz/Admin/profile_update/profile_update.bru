meta {
  name: profile_update
  type: http
  seq: 1
}

put {
  url: {{base_url2}}/admin/users/profile_update
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  name: shruti Patil
}

settings {
  encodeUrl: true
  timeout: 0
}
