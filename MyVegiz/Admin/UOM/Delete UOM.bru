meta {
  name: Delete UOM
  type: http
  seq: 4
}

delete {
  url: {{base_url2}}/admin/uoms/delete?uom_id=12
  body: none
  auth: bearer
}

params:query {
  uom_id: 12
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
