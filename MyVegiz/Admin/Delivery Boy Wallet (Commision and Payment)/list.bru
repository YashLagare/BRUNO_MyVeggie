meta {
  name: list
  type: http
  seq: 1
}

get {
  url: {{base_url2}}/admin/delivery_boy_wallet/list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
