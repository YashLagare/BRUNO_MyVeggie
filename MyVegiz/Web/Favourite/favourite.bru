meta {
  name: favourite
  type: http
  seq: 1
}

post {
  url: {{base_url2}}/web/favourites/toggle?product_id=1021
  body: json
  auth: bearer
}

params:query {
  product_id: 1021
}

auth:bearer {
  token: {{web_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
