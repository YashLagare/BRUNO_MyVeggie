meta {
  name: list
  type: http
  seq: 1
}

get {
  url: {{base_url}}web/web_slider/list?page=1&limit=10
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 10
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
