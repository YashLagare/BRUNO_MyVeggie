meta {
  name: list
  type: http
  seq: 3
}

get {
  url: {{base_url2}}/web/profile/address/list
  body: json
  auth: bearer
}

auth:bearer {
  token: {{web_access_token}}
}

body:json {
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}
