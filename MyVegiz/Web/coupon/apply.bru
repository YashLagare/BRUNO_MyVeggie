meta {
  name: apply
  type: http
  seq: 1
}

post {
  url: {{base_url2}}/web/cart/coupon/apply
  body: json
  auth: bearer
}

auth:bearer {
  token: {{web_access_token}}
}

body:json {
  {
    "coupon_code": "SAVE611"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
